import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    TestsList: {
            width: '100%',
    },
    testsList: {
        height: '500px',
        width: '100%',
    },
});

export default useStyles;
