import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    TestsGroupForm: { width: 'min-content', display: 'flex', flexDirection: 'row', alignItems: 'center'},
    ButtonRoot: {
        minWidth: '20px',
        minHeight: '20px',
    },
});

export default useStyles;
