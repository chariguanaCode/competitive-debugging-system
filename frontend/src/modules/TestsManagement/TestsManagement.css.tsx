import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    TestsManagement: {
        width: '100%', 
        height: '100%', 
        padding: '15px'
    },
});

export default useStyles;
