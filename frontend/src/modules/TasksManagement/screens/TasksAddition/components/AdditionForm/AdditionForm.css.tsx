import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    AdditionForm: { width: '50%', height: '100%' },
    titleContainer: {},
    selectFilesForm: {},
    selectedFilesListContainer: {width: '100%', height: '50%'},
    pendingFilesListContainer: {width: '100%', height: '50%'},
});

export default useStyles;
