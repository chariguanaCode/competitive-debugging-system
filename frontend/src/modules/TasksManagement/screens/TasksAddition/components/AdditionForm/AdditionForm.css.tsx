import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    AdditionForm: {},
    titleContainer: {},
    selectFilesForm: {},
    selectedFilesListContainer: {

    }
});

export default useStyles;
