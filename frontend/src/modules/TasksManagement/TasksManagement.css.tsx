import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    TasksManagement: {
        width: '1000px', // DEV
        height: '700px', // DEV
        margin: '50px', // DEV
    },
});

export default useStyles;
