import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    wrapper: {
        width: '100%',
        height: '100%',
        userSelect: 'none',
        padding: 10,
    },
});

export default useStyles;
